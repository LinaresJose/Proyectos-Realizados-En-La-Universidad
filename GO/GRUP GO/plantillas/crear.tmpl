{{define "crear"}}
    {{template "cabecera"}}
    <br/>
    <div class="card">
        <div class="card-header">
            Crear Empleado
        </div>
        <div class="card-body">
            <form method="POST" action="/insertar">
                <div class="form-group">
                  <label for="">Nombre:</label>
                  <input type="text" name="nombre" id="nombre" class="form-control" placeholder="Escribe tu nombre" aria-describedby="helpId" required>
                </div>
                <div class="form-group">
                  <label for="">Correo:</label>
                  <input type="email" name="correo" id="correo" class="form-control" placeholder="Escribe tu correo" aria-describedby="helpId" required>
                </div>
                <button type="submit" class="btn btn-success">Agregar Empleado</button>
                <a href="/" class="btn btn-danger">Cancelar</a>
            </form>
        </div>
    </div>
    {{template "pie"}}
{{end}}